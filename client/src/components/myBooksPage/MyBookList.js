import UserDetails from './UserDetails'


const MyBookList = () => {

    // const bookEls = users.map((users, i) => {
    //     return (<UserDetails key={i} username={users.username} community={users.community} />);
    // });
    
    return (
        <>
        <h1>this is my book list</h1> 
        <UserDetails />
        </>
    )
}

export default MyBookList